// AI that just stands still

function getMove(myShip, otherShip, myBullets, otherBullets, asteroids) {
    return {
        forward : false,
        fire : false,
        rotateLeft : false,
        rotateRight : false
    }
}
